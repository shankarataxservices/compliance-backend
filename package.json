{
  "name": "compliance-backend",
  "version": "1.0.0",
  "dependencies": {
    "busboy": "^1.6.0",
    "csv-parse": "^5.5.6",
    "exceljs": "^4.4.0",
    "firebase-admin": "^12.0.0",
    "googleapis": "^134.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  },
  "scripts": {
    "build": "esbuild netlify/functions/*.js --bundle --outdir=build --platform=node"
  }
}
